# R5 — present re-arm: enters de `func_00017ACC` + callers (pre vs post R_Perm)

**Data:** 2026-07-23  
**Tipo:** diagnóstico only (sem fix de flip HLE).  
**Gate B:** GREEN · **Gate A:** RED  
**Log canónico:** `/tmp/r5_17acc.log`  
**Recipe:** `TIMEOUT=90 LOG=/tmp/r5_17acc.log PS3_NO_RSX=1 PS3_TRACE_17ACC=1 ./rodar_gow2_menu_fast.sh`

Prior: R3 (MAINLOOP `g={17ADE,17ADF}` como fingerprint único de flip pré-WAD).

---

## Probe shipped

| Artefacto | Função |
|-----------|--------|
| `recomp_mid_v2/patch_17acc_enter_probe.py` | Idempotente; inject em entry de 17ACC + 9 callers estáticos |
| Gate | `PS3_TRACE_17ACC=1` (first char `'1'`, default OFF) |
| post window | `g_ps3_rperma_full` (já setado em `movie_hle.c` no R_PermA full) — **sem** change em ps3recomp |
| Log | `[17ACC] probe armed` · `[17ACC] enter fn=… tot= post= r3= r4=` (cap 8/4) · `[17ACC] SUMMARY` (atexit + SIGTERM) |

Sites (lift `recomp_macos_v2`):

| id | fn | ficheiro |
|---:|----|----------|
| 0 | `func_00017ACC` | `ppu_recomp_000.cpp` |
| 1–5 | `1E1A8` `1E34C` `1EAD8` `25064` `25614` | 000 |
| 6–9 | `1E3DC` `1E3E8` `1E400` `1E4AC` | 001 |

---

## Smoke metrics

| Métrica | Valor |
|---------|------:|
| R_Perm full (20169344) | 1 |
| thr_auto_load end | 1 |
| SetFlip_total | 3743 |
| SetFlip_after_R_Perm | **0** |
| Pad_after_R_Perm | 0 |
| ICALL_BAD_after_R_Perm | 12 |
| Gate B | **GREEN** |
| Gate A | **RED** |

---

## Contagens enter (pre / post)

| fn | tot | post | pre | Notas |
|----|----:|-----:|----:|-------|
| **17ACC** | **0** | **0** | 0 | Nunca entra no menu_fast |
| 1E1A8 | 0 | 0 | 0 | caller estático |
| 1E34C | 0 | 0 | 0 | |
| 1EAD8 | 0 | 0 | 0 | |
| 25064 | 0 | 0 | 0 | |
| 25614 | 0 | 0 | 0 | |
| 1E3DC | 0 | 0 | 0 | fragmento 001 |
| 1E3E8 | 0 | 0 | 0 | |
| 1E400 | 0 | 0 | 0 | |
| 1E4AC | 0 | 0 | 0 | |
| **grand** | **0** | — | — | `rperma_full=1` no SUMMARY |

**Zero** linhas `[17ACC] enter` no boot inteiro (pré e pós thr).

Cross-check (sessão): force-log **sem gate** em `17A94`/`17AF4`/`17ACC`/`1EAD8`/`25064` — também **0** enters, com `SetFlip` ~2k e MAINLOOP `g={17ADE,17ADF}` ainda presente.

---

## Interpretação

1. **17ACC tot=0 com SetFlip≫0** ⇒ o path recompilado `func_00017ACC` (+ callers listados) **não** é o issuer de flip neste lift/boot. Não é só “falta de re-arm pós thr”: o path **nunca** agenda pre-WAD via estas C functions.
2. O fingerprint MAINLOOP `g={0x17ADE,0x17ADF}` (EAs **consecutivos**, `lr=0`) é **stack residual** no scan de `ps3_hle_call` (heurística de palavras no guest SP), **não** prova de execução viva de `func_00017ACC`. R3 D5 sobre “único path 17ACC band” fica **enfraquecido** / reclassificado.
3. `post=0` com `rperma_full=1` é **vácuo trivial** (tot=0). Não discrimina “re-arm nunca corre” vs “issuer é outro”.
4. Gate A continua RED pela ausência de SetFlip/Pad pós R_Perm (independente deste path).

### Callers pre-WAD only? post thr?

| Pergunta | Resposta |
|----------|----------|
| Quais callers disparam só pre-WAD? | **Nenhum** — nenhum dispara |
| Algum post thr / post R_Perm? | **Não** |
| 17ACC tot≫0 mas post=0? | **Não** — tot=0 |

---

## R6 (próximo gate)

**Alvo:** identificar o **issuer real** de `cellGcmSetFlipCommand` no host stack / guest CIA verdadeiro (não o scan residual de SP).

Sugestões (por ordem de custo):

1. Em `ps3_hle_call` / `cellGcmSetFlipCommand`: capturar `ctx->cia` se o lifter o mantém, ou host backtrace macOS (`backtrace`) nos primeiros N flips — mapear símbolo host → função recomp.
2. Instrumentar `ps3_import_thunk` / dispatch HLE com contagem por **caller host** (return address) para o NID `0x5C770579`.
3. Só depois: se o issuer real for outro `func_*`, repetir a matriz pre/post R_Perm nesse site (re-arm).

**Não** forjar flip HLE. **Não** assumir re-arm de 17ACC sem prova de enter.

---

## Commits

- gow2: `diag(gow2): R5 probe enter 17ACC/callers pre-vs-post R_Perm`
- ps3recomp: **não necessário** (`g_ps3_rperma_full` já existe)
