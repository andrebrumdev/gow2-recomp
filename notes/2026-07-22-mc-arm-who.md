# Who should arm orphan micro-ctor OPD `0x534CB8`? (2026-07-22)

## Goal
Answer **who should arm** the micro-ctor path (`OPD 0x00534CB8` → `func_0032854C` family)
on the natural post-R_Perm boot graph. **Not** SHADERSRC / ICGLdr natural acceptance.

**Proof (scratch):**
- `{SCRATCH}/mc_arm_static.txt` — static arming census
- `{SCRATCH}/mc_arm_inboot.log` — natural boot (~R_Perm FULL + post)
- `{SCRATCH}/mc_arm_counts.txt` — probe counts

**Recipe:** `env_gow2.sh` + `PS3_NO_RSX=1` + `PS3_TRACE_MC=1 PS3_TRACE_A1CHAIN=1
PS3_TRACE_TYMAP=1 PS3_TRACE_TYMAP_DUMP=1 PS3_TRACE_LDRSH=1`. No `PS3_GATE_FORCE`
synthetic WAD stream, no CRC bypass, no `PS3_SHADER_DEMO`.

---

## Criterion 1 — conclusion (**1b negative**)

**No natural arming site exists** for OPD `0x534CB8` / code `0x32854C` in this EBOOT
or in the natural post-R_Perm boot graph.

| Claim | Evidence |
|-------|----------|
| Static orphan | Every arming edge class below is **absent** |
| Runtime never | `[MC-32854C]`…family **0**; `[MC-SCAN] hits=0`; `[OPD-ICG]` **0** |
| Typemap orthogonal | `[TYMAP-DUMP]` live `vt+8=0x522E70` without this family (built-never-walked) |

There is **no** “missing `bl`” or “missing vtable slot load” to restore in the
current image for this OPD: the OPD **exists** in `.opd`, but **nothing in code or
data tables points at it**.

---

## Criterion 2 — static arming census

| Edge class | Result | Anchor / search |
|------------|--------|-----------------|
| OPD present in EBOOT | **yes** | `file+0x524CB8` code=`0x32854C` toc=`0x541178` (guest OPD `0x534CB8`) |
| Direct `bl`/`b` → `0x0032854C` | **0** | PPC I-form scan guest text `0x10000..` |
| Direct `bl`/`b` → `0x00328B18` | **0** | same |
| `bl` → `0x00329490` | **2** | only **inside** family (`0x3286AC`, `0x328C74`) — not external arm |
| `bl` → `0x00468C3C` | **2** | only inside family (`0x329618`, `0x3297E0`) |
| BE literal `0x00534CB8` anywhere | **0** | full EBOOT `struct.pack('>I')` count |
| BE literal `0x00534CC0` / `0x00534D70` | **0** | same (sibling OPDs also unpointed as absolutes) |
| `lis`/`addi` pair materializing `0x534CB8` | **0** | addis imm=`0x53` + addi imm=`0x4CB8` pairs |
| `lis`/`addi` pair materializing `0x32854C` | **0** | addis `0x32` + addi `0x854C` |
| TOC-rel `r2 + (-0xC4C0)` (if TOC=`0x541178`) | **0** | no lift imm match for that offset |
| Lift callers of `func_0032854C` | **0** | only def `ppu_recomp_001.cpp` + registry `030` |
| Lift callers of intermediate factories | closed under family | `0032862C/678/BF8/C40` → `00329490`; roots still under orphan OPD |
| OPD ptr in **data/vtable tables** outside `.opd` | **0 for 0x534CB8** | full-file scan excluding `file 0x520000–0x540000` |
| Neighbor OPD ptrs in factory tables | **yes (others)** | e.g. guest `0x518560+` has `0x534C50`, `0x534C60`, then **skips** `0x534CB8`, later `0x534D08`… |

**Table gap (important):** at guest `0x00518560`… the static function-pointer cluster
lists many OPDs in the `0x534Cxx` range but **never** `0x534CB8`. Closest values are
`0x534C60` (dist 88) and earlier slots — the micro-ctor root is a **hole**, not a
load-time unresolved slot.

Internal family edges (for orientation only — do **not** arm the root):

```
OPD 0x534CB8 → func_0032854C   ← NO external edge
        ├─ … → func_00328B08 / 00328978 / …
        └─ … → func_00329490 → func_00468C3C → (hash path; prior: dead li r30,0)
```

`GroupStartCtor` / typemap install (`func_00294004` via `002B133C`) is **orthogonal**
and already runs (`TYMAP-DUMP`); it does not call `0032854C`.

---

## Criterion 3 — in-boot natural reconfirm

Log: `mc_arm_inboot.log` (boot_gow2, probes ON, no synthetic GATE stream).

| Probe | N | Meaning |
|-------|--:|---------|
| R_Perm FULL `20169344` | ≥1 | WAD delivered |
| **`[MC-32854C]`** | **0** | root never entered |
| MC-328B08 / 328978 / 328B18 / 329490 / … | **0** | family never |
| **`[MC-SCAN] done hits=0`** | 1 | no OPD/code ptr outside `.opd` after R_Perm |
| `[OPD-ICG]` 534CB8 / 32854C | **0** | no `ps3_call_opd` of root |
| `[A1-468C3C]` | **0** | hashmap set never |
| `[TYMAP-171]` | **0** | walk never |
| `[LDRSH]` | **0** | ICGLdr natural never |
| `[TYMAP-DUMP]` | **1** | `vt=0x5130B8 vt+8=0x522E70` live |
| `GATE-FORCE calling` (synthetic walk) | **0** | not used as pass |
| `PS3_SHADER_DEMO` | **0** | not used |

```
[TYMAP-DUMP] base=0x00700DF8 slot=0x00835778
  comp=0x4306ADF0 tm=0x4306B160 vt=0x005130B8 vt+8=0x00522E70
[MC-SCAN] searching for OPD 0x00534CB8 / code 0x0032854C / OPD 0x00534D70
[MC-SCAN] done hits=0
```

Note: log line `[GATE-FORCE] R_PermA full flagged` is a **name tag** for R_Perm
completion, not `PS3_GATE_FORCE` synthetic stream / walk.

---

## Criterion 4 — SHADERSRC not acceptance

| Signal | N | Role |
|--------|--:|------|
| TYMAP-171 / LDRSH | 0 | deferred (schedule unarmed) |
| SHADERSRC / HOSTRES lines | may exist | **not** arming of micro-ctor; **not** Wall D green |

---

## Pass/fail

| Criterion | Result |
|-----------|--------|
| 1 Arming answered (1a or 1b) | **PASS (1b)** |
| 2 Static census table | **PASS** (`mc_arm_static.txt`) |
| 3 In-boot MC still 0 + R_Perm | **PASS** (`mc_arm_inboot.log`) |
| 4 Not SHADERSRC-as-accept | **PASS** |

---

## Single next discriminator (not SHADERSRC green)

Because **nobody in this image arms `0x534CB8`**, chasing a caller of `0032854C` is
closed for the current ELF.

**Next gate (pick one, in order of value):**

1. **Alternate walk arming (preferred for Wall D):** find who **vcalls typemap
   `vt+8` / OPD `0x522E70`** without the micro-ctor family (component stream,
   menu/scene object after further FSM). Discriminate with longer boot + probes
   on `ps3_call_opd` / live vtable slot use — still **no** GATE synthetic as accept.
2. **Later-phase registration only if it appears:** if a future menu/level path
   ever **writes** `0x534CB8` into heap (MC-SCAN would go hits≠0), name that writer
   then. Current window: hits=0 after R_Perm FULL + TOC/SHGX expand.
3. **Do not** forge `ps3_call_opd(0x534CB8)` or force `func_0032854C` as product
   acceptance.

---

## Non-regression

- R_Perm FULL `bytes_read=20169344` observed.
- Typemap object still dumps correctly post-R_Perm.
- Probes remain env-gated OFF by default.
- No SHADERSRC/GATE/CRC claimed as pass.

## Related notes
- `2026-07-22-opd-534cb8-dead.md` — prior dead-OPD proof
- `2026-07-22-mc-schedule-climb.md` — climb graph
- `2026-07-22-wall-d-schedule-map.md` — schedule map / next gate framing
- `2026-07-22-wall-d-goal-verdict.md` — built-never-walked reconfirm
