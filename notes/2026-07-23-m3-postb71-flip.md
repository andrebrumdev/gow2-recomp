# M3 — Post-B71 SetFlip (Gate A partial) (2026-07-23)

**Tipo:** diagnóstico + métricas automatizadas. **Gate A continua RED.**  
**Não claim:** menu / jogável / SetFlip_after_R_Perm ≥ 1.

## Infra (shipped)

| Artefacto | Função |
|-----------|--------|
| `count_menu_gate.py` | CLI estável: `SetFlip_after_R_Perm`, `Pad_after_R_Perm`, Gate A/B |
| `rodar_gow2_menu_fast.sh` footer | mesmos contadores pós-log + chama o companion |
| `recomp_mid_v2/patch_cc9d0_live_yield.py` | inject gated em `func_000CC9D0` (lift local) |

Env (default **OFF**):

| Var | Efeito |
|-----|--------|
| `PS3_CC9D0_YIELD=1` | yield giant-lock + usleep a cada 1024 ticks de produto **live** |
| `PS3_CC9D0_LIVE_SKIP=1` | early-return do body (como pin-shell SKIP) no produto live |

## Recipe

```bash
# Baseline Gate A/B
TIMEOUT=90 LOG=/tmp/m3_baseline_norsx.log PS3_NO_RSX=1 ./rodar_gow2_menu_fast.sh
python3 count_menu_gate.py /tmp/m3_baseline_norsx.log

# Disc: yield live
TIMEOUT=90 LOG=/tmp/m3_yield.log PS3_NO_RSX=1 PS3_CC9D0_YIELD=1 ./rodar_gow2_menu_fast.sh

# Disc: skip live body
TIMEOUT=100 LOG=/tmp/m3_liveskip.log PS3_NO_RSX=1 PS3_CC9D0_LIVE_SKIP=1 ./rodar_gow2_menu_fast.sh
```

Backend medido: **`PS3_NO_RSX=1`** (agent headless). SetFlip é log guest em
`cellGcmSetFlipCommand` — **independe de Metal** (M0 já tinha 3k+ flips pré-R_Perm
sob none). Metal não re-medido nesta task (janela GUI no agente); se flip só
existisse sob Metal, isso invalidaria NO_RSX — **não é o caso**.

## Contagens (in-boot)

| Run | thr | R_Perm | FATAL | PARK | SetFlip_total | **SetFlip_after_R_Perm** | Pad_after | ICALL_BAD_after | Gate B | Gate A |
|-----|-----|--------|-------|------|---------------|--------------------------|-----------|-----------------|--------|--------|
| Baseline NO_RSX | 1 | 1 | 0 | 0 | 3815 | **0** | 0 | 12 | GREEN | **RED** |
| YIELD=1 (retry) | 1 | 1 | 0 | 0 | 23253 | **0** | 0 | 12 | GREEN | **RED** |
| LIVE_SKIP=1 | 1 | 1 | 0 | 0 | 26452 | **0** | 0 | 12 | GREEN | **RED** |

CLOSE-PRESERVE ×2, attach=full ×2 em todos os runs Gate B green (M2 não regrediu).

1ª tentativa YIELD flaky (sem R_Perm) — retry OK (política thr flake).

### Quotes

```
[TYPE15] product list CLOSE-PRESERVE prod=0x42F85AE4 head=0x42F85F10 … nodes=12
[TYPE15] CC9D0-YIELD this=0x4066D804 prod=0x42F85AE4   # YIELD run
[TYPE15] CC9D0-LIVE-SKIP this=0x4066D798 prod=0x42F85AE4
[TYPE15] CC9D0-LIVE-SKIP this=0x4066D804 prod=0x42F85AE4
[ICALL-BAD] ctr=0x40637488 lr=0x00000000 r2=0x40637478 r3=0x400C3D88 r12=0x40004024
[MOVIEFSM] st620 0 -> 0 … overlay_done=1   # pós thr, só isto
```

## Timeline crítica (baseline)

Último `SetFlipCommand` ocorre **antes** do open de `R_LglScA` / stream de
`R_PermA` — não só “depois do full”:

| Ordem | Evento |
|------:|--------|
| … | SetFlip bufferId=0/1/2 (último flip do boot) |
| +1 | `movieio open '/wad/r_lglsca.wad_ps3'` |
| … | R_Perm open + pump full 20169344 |
| … | ICALL-BAD ×12 (`ctr=0x40637488`) |
| … | TYPE15 CLOSE-PRESERVE + CB56C attach=full ×2 |
| … | thr_auto_load start/end |
| … | CC9D0 ticks (f4=f54=0, 2 this); st620 0→0 |
| timeout | **zero** SetFlip / cellPadGetData após linha R_Perm full |

## Discriminadores (M3 brief)

| Exp | Resultado | Interpretação |
|-----|-----------|---------------|
| Yield live product | YIELD loga; SetFlip_after=0 | **H4 starvation DEAD** de novo (pós-M2 list) |
| LIVE_SKIP body | LIVE-SKIP loga nos 2 this; SetFlip_after=0 | Parent **não** usa o body de CC9D0 para chegar a flip — skip barato não desbloqueia present |
| NO_RSX vs Metal | NO_RSX suficiente para o metric (printf guest) | Gate A medido sob none |

## Root class (honest)

**Classe primária: guest abandona o path de present/frame flip ao sair do
modo movie/logo para o load de WADs e nunca reentra em `cellGcmSetFlip*` até
timeout.**

Não é (sozinho):

1. backend silencioso (SetFlip loga milhares de vezes no intro),
2. giant-lock / falta de yield no tick live TYPE15 (YIELD e LIVE_SKIP medidos),
3. lista vazia (M2: nodes=12, child_head real) — **necessário mas não suficiente**.

**Co-sinais (não desbloqueados nesta task):**

| Sinal | Nota |
|-------|------|
| f4/f54 eternamente 0 | estado idle do tick TYPE15; CB56C zera 1×; writer de avanço (enum 1/5/6/…) não corre |
| só 2 `this` CC9D0 | parent scene list não cresce pós-B71 |
| ICALL-BAD `0x40637488` ×12 pós-R_Perm | icall heap/vtable com cookies `0x40004024` — candidato a path de UI/render partido |
| st620 0→0 pós thr | 2º movie / title FSM idle |

## Pass / fail M3

| Critério | Status |
|----------|--------|
| Footer / `count_menu_gate.py` automatiza SetFlip_after_R_Perm | **PASS** |
| SetFlip_after_R_Perm ≥ 1 **ou** present path equivalente documentado com prova | **FAIL** (0; sem equivalent honest) |
| Gate B green | **PASS** (nos runs que atingem WAD; 1 flake documentado + retry) |
| Sem PARK / forge / claim menu | **PASS** |

**Veredito task:** `DONE_WITH_CONCERNS` — infra + root class; **não** commitar
como `fix(gow2): frame loop…` com Gate A green.

## Próximo (M3+/M4, fora desta entrega)

1. RE de quem **deveria** chamar `cellGcmSetFlip` no frame loop pós-WAD (stack
   do último flip pré-WAD com `PS3_TRACE_MAINLOOP` se portado a Darwin, ou
   caller estático do HLE path).
2. Tratar `ICALL-BAD 0x40637488` (origem do ctr / freelist cookie em r12).
3. Quem escreve f4∈{1,5,6,7,8,9} no fluxo natural (não forjar em UNSTICK).
4. **Não** promover `PS3_CC9D0_LIVE_SKIP` a default — provado inútil para flip.

## Constraints honrados

- Kill por PID (script menu-fast).
- Probes OFF default; skip/yield só disc.
- Sem PARK / StartSeq forge / menu claim.
- Sem dados de jogo no commit.
